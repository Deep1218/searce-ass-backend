let globalConfig = {};

//* User role constant
//! note: if you change here then change database to
globalConfig.dbCollection = {
  USERS: "users",
  PROJECTS: "projects",
};

module.exports = globalConfig;
